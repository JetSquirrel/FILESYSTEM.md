---
import { getEntry, render } from "astro:content";
import DocPage from "@/layouts/DocPage.astro";

const doc = await getEntry("docs", "compare");

if (!doc) {
	throw new Error("Compare doc is missing");
}

const { Content } = await render(doc);
---

<DocPage doc={doc}>
	<Content />
</DocPage>
