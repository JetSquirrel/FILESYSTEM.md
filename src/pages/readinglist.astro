---
import { getEntry, render } from "astro:content";
import DocPage from "@/layouts/DocPage.astro";

const doc = await getEntry("docs", "readinglist");

if (!doc) {
	throw new Error("Reading list doc is missing");
}

const { Content } = await render(doc);
---

<DocPage doc={doc}>
	<Content />
</DocPage>
