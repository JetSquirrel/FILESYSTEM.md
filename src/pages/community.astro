---
import { getEntry, render } from "astro:content";
import DocPage from "@/layouts/DocPage.astro";

const doc = await getEntry("docs", "community");

if (!doc) {
	throw new Error("Community doc is missing");
}

const { Content } = await render(doc);
---

<DocPage doc={doc}>
	<Content />
</DocPage>
